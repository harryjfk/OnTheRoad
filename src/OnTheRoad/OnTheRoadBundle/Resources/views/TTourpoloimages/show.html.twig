{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TTourpoloimages</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Pathimage</th>
                <td>{{ entity.pathimage }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ttourpoloimages') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ttourpoloimages_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
