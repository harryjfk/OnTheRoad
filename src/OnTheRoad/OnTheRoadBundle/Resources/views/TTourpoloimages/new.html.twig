{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TTourpoloimages creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('ttourpoloimages') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
